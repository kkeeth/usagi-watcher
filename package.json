{
  "name": "usagi-watcher",
  "version": "1.0.0",
  "description": "ウサギに関する投稿をいつも見ている",
  "main": "index.js",
  "scripts": {
    "serve": "npm --prefix=functions run serve",
    "build": "npm --prefix=functions run build",
    "deploy": "npm --prefix=functions run deploy"
  },
  "author": "kkeeth",
  "license": "ISC",
  "devDependencies": {
    "eslint-config-google": "^0.14.0",
    "firebase-tools": "^11.8.0",
    "ngrok": "^3.3.0"
  },
  "private": true
}
